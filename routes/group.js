const express = require('express');
const router = express.Router();

const group_controller = require("../controllers/group.js");

// get, post, put, delete 개발
// router.post('/', group_controller.create);

// router.get('/all', group_controller.findAll);

// router.get('/', group_controller.findOne);

// router.get('/:user_cd', group_controller.findAllByUser);

// router.delete('/', group_controller.delete);

module.exports = router;
